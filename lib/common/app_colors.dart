import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales selon votre palette
  static const Color primary = Color(0xFFE07A5F); // Boutons, barres de navigation
  static const Color secondary = Color(0xFF81B29A); // Surlignages, liens actifs
  static const Color background = Color(0xFFF4F1DE); // Arrière-plan global, cartes
  static const Color textPrimary = Color(0xFF3D405B); // Titres, paragraphes
  static const Color textSecondary = Color(0xFF2E294E); // Légendes, placeholders
  static const Color accent = Color(0xFFF2CC8F); // Pied de page, menus contextuels
  
  // Couleurs supplémentaires
  static const Color cardBackground = Colors.white;
  static const Color shadow = Color(0x1A000000);
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFF9800);
}
